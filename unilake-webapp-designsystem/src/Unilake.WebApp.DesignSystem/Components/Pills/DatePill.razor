@inherits UnilakeBaseComponent
@namespace Unilake.WebApp.DesignSystem.Components

@* todo: add correct culture here *@
<div class="@ClassNames">
 <Icon IconInstance="AnronIcons.Calendar" Size="16"/>
 <p>
  @Value.Date.ToString("dd MMMM yyyy")
 </p>
</div>

@code {
 [Parameter, EditorRequired] public required DateTime Value { get; init; }
 protected override string ClassNames => ClassBuilder
  .Add("flex gap-1 px-2 items-center w-fit rounded-md bg-brand-bravo-100 text-feedback-info-text")
  .ToString();
}
